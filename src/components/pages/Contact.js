import React from 'react'
import ShowProps from '../ShowProps'

const Contact = props => (
  <div className="Cnt">
    <h2>[ Contact ]</h2>
    <ShowProps {...props} />
  </div>
)

export default Contact
