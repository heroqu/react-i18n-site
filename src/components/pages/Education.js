import React from 'react'
import ShowProps from '../ShowProps'

const Education = props => (
  <div className="Cnt">
    <h2>[ Education ]</h2>
    <ShowProps {...props} />
  </div>
)

export default Education
